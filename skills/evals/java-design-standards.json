{
  "skill_name": "java-design-standards",
  "evals": [
    {
      "id": 1,
      "prompt": "这个类设计有什么问题？\n\n```java\npublic class OrderService {\n    public void createOrder() { /* 创建订单 */ }\n    public void sendEmail() { /* 发送邮件 */ }\n    public void generateReport() { /* 生成报表 */ }\n    public void updateUserProfile() { /* 更新用户资料 */ }\n    public void processPayment() { /* 处理支付 */ }\n}\n```",
      "expected_output": "应该指出：1) 违反了单一职责原则，一个类承担了太多职责；2) 应该按职责拆分：OrderService（订单）、EmailService（邮件）、ReportService（报表）、UserService（用户）、PaymentService（支付）；3) 类设计应该易于维护和扩展"
    },
    {
      "id": 2,
      "prompt": "订单有这么多状态，如何清晰表达？\n\n订单状态：已下单、待付款、已付款、待发货、已发货、已收货、已取消、已退款",
      "expected_output": "应该指出：1) 如果业务对象状态超过3个，应该使用状态图来表达；2) 明确状态变化的各个触发条件；3) 明确哪些状态之间不能直接转换（如已下单不能直接跳到已收货）；4) 可以使用枚举类定义状态，并定义状态转换规则"
    },
    {
      "id": 3,
      "prompt": "这段代码违反了哪些设计原则？\n\n```java\npublic class PaymentProcessor {\n    // 直接依赖具体实现\n    private AlipayService alipayService = new AlipayService();\n    private WechatPayService wechatPayService = new WechatPayService();\n    \n    public void pay(String type, double amount) {\n        if (\"alipay\".equals(type)) {\n            alipayService.pay(amount);\n        } else if (\"wechat\".equals(type)) {\n            wechatPayService.pay(amount);\n        }\n        // 如果要加新的支付方式，必须修改这个类\n    }\n}\n```",
      "expected_output": "应该指出：1) 违反了依赖倒置原则，应该依赖抽象接口而不是具体实现；2) 违反了开闭原则（对扩展开放，对修改闭合），新增支付方式需要修改现有代码；3) 应该定义 PaymentService 接口，通过依赖注入实现；4) 可以使用策略模式或工厂模式优化"
    }
  ]
}
