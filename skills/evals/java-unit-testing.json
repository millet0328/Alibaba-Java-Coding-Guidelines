{
  "skill_name": "java-unit-testing",
  "evals": [
    {
      "id": 1,
      "prompt": "这个单元测试有什么问题？\n\n```java\npublic class UserServiceTest {\n    \n    @Test\n    public void testGetUser() {\n        UserService service = new UserService();\n        User user = service.getUser(1L);\n        System.out.println(user.getName());\n    }\n    \n    @Test\n    public void testCreateUser() {\n        testGetUser();\n        UserService service = new UserService();\n        boolean result = service.createUser(new User());\n        assertTrue(result);\n    }\n}\n```",
      "expected_output": "应该指出：1) 单元测试必须使用 assert 验证，不能用 System.out 人肉验证；2) 测试用例之间不能互相调用，必须保持独立性；3) 单元测试应该全自动执行，非交互式；4) 测试代码应写在 src/test/java 目录下；5) 应该使用 Mock 框架隔离外部依赖"
    },
    {
      "id": 2,
      "prompt": "如何编写一个符合 AIR 原则的单元测试？给我一个示例。",
      "expected_output": "应该解释 AIR 原则：A-Automatic（自动化）、I-Independent（独立性）、R-Repeatable（可重复），并给出示例：使用 @Test 注解、使用 Mock 隔离依赖、使用 assert 验证结果、测试方法之间无依赖、可以重复执行"
    },
    {
      "id": 3,
      "prompt": "这个数据库测试有什么问题？\n\n```java\n@Test\npublic void testDeleteUser() {\n    // 手动在数据库插入测试数据\n    userDao.deleteUser(999L);\n    User user = userDao.getUser(999L);\n    assertNull(user);\n}\n```",
      "expected_output": "应该指出：1) 不能使用假设数据库里已有数据，应该用程序插入测试数据；2) 应该设定自动回滚机制，避免脏数据；3) 测试数据应该有明确的前后缀标识，如 TEST_USER_999；4) 测试粒度应该足够小，一般是方法级别"
    }
  ]
}
